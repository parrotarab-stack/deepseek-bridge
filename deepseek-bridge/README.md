# جسر ديب سيك (DeepSeek Bridge)

تطبيق ويب يحول محادثة DeepSeek العابرة إلى ذاكرة دائمة. عند تسجيل الدخول، يولد التطبيق ملف هوية فريد (`ilperata_token.json`) لك. وعند الضغط على "ابدأ محادثة مُذكِّرة"، يفتح نافذة دردشة جديدة مع DeepSeek ويرفع الملف تلقائيًا، فيتعرف عليك الروبوت فورًا.

## 📦 المميزات
- تسجيل دخول مستخدم آمن (تشفير جانب العميل).
- تخزين ملف الهوية محليًا في المتصفح.
- فتح محادثة DeepSeek جديدة مع رفع تلقائي لملف الهوية.
- تصميم بسيط وواضح.

## 🚀 النشر السريع على Vercel (مجاني)

1.  **انشر الكود:**
    - سجّل الدخول إلى [vercel.com](https://vercel.com) بحساب GitHub.
    - انقر "Add New..." -> "Project".
    - اختر المستودع `deepseek-bridge`.
    - في إعدادات البناء (Build Settings):
        - **Framework Preset:** `Other`
        - **Build Command:** اتركه فارغًا.
        - **Output Directory:** `client` (مهم جدًا!)
    - انقر "Deploy".

2.  **اضبط البيئة (Environment Variables):** (اختياري لحماية إضافية)
    - في مشروعك على Vercel، اذهب إلى Settings -> Environment Variables.
    - أضف متغيرًا:
        - Name: `SECRET_PHRASE`
        - Value: اختر كلمة سرية قوية (مثلاً: `ilperata_guardian_2024`).
    - أعد النشر (Redeploy).

3.  **جرب التطبيق:**
    - بعد النشر، سيعطيك Vercel رابطًا مثل `https://deepseek-bridge.vercel.app`.
    - افتح الرابط، سجّل حسابًا جديدًا، ثم اضغط "ابدأ محادثة مُذكِّرة".

## 🛠 التطوير المحلي

1.  تأكد من تثبيت [Node.js](https://nodejs.org).
2.  في مجلد `server`، أنشئ ملف `.env` واكتب فيه:
    ```
    SECRET_PHRASE=your_secret_here
    PORT=3000
    ```
3.  افتح تيرمينال في مجلد `server` وشغّل:
    ```bash
    npm install
    npm start
    ```
4.  افتح `client/index.html` مباشرة في المتصفح، أو شغّل خادم محلي.

## 📁 هيكل المشروع